# Relay Morning Pipeline Review — 2026-04-01

**Status: BLOCKED — API access unavailable in isolated cron context**

## Issues Encountered

1. **relay.md not found** — `/Users/kurtishon/clawd/agent-prompts/relay.md` does not exist. Cannot load full instructions.

2. **exec tool requires interactive approval** — All `curl` commands to `vend.kandedash.com` were blocked pending `/approve`. Isolated cron sessions have no user present to approve these.

3. **web_fetch cannot pass API key headers** — Returns 401 Unauthorized.

## Impact

- Mixmax sync was NOT performed
- Prospects were NOT pulled
- Activities were NOT pulled
- No pipeline analysis could be completed this morning

## Recommended Fix

To unblock Relay morning/evening crons:
- Pre-approve curl commands to `vend.kandedash.com` with `allow-always` policy in exec tool settings
- OR recreate `/Users/kurtishon/clawd/agent-prompts/relay.md` (currently missing)
- Set exec `ask=off` or `security=full` policy for known internal API endpoints in cron context
